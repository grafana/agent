scrape_configs:
  - job_name: fun
    azure_sd_configs:
      - subscription_id: "subscription"
        tenant_id: "tenant"
        client_id: "client"
        client_secret: "secret"
tracing: {enabled: false}
